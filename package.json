{
  "name": "coralsoft-chat-proxy",
  "version": "1.0.0",
  "description": "Local proxy to forward CoralSoft assistant requests to Anthropic securely and serve the site locally",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "pages:login": "wrangler login",
    "pages:dev": "wrangler pages dev .",
    "pages:deploy": "wrangler pages deploy ."
  },
  "dependencies": {
    "axios": "^1.4.0",
    "dotenv": "^16.0.0",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "wrangler": "^4.99.0"
  }
}
